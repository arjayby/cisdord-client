export type ChannelType = {
  id: number;
  shortId: string;
  userId: number;
  name: string;
  description: string;
  tags: string[];
  membersCount: number;
  createdAt: string;
  updatedAt: string;
};
